name: release-candidate-ready-issuance-unblocking-request

on:
  push:
  pull_request:

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: python3 -m unittest tests/test_release_candidate_ready_issuance_unblocking_request.py
      - run: bash scripts/verify-release-candidate-ready-issuance-unblocking-request.sh
