name: authority-object-admission-intake-reopening-request-decision-ledger

on:
  pull_request:
  push:

jobs:
  authority-object-admission-intake-reopening-request-decision-ledger:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Verify authority object admission intake reopening request decision ledger
        run: bash scripts/verify-authority-object-admission-intake-reopening-request-decision-ledger.sh

      - name: Test authority object admission intake reopening request decision ledger
        run: python3 -m unittest tests/test_authority_object_admission_intake_reopening_request_decision_ledger.py
