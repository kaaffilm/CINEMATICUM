name: real-case-authority-object-admission-decision-ledger

on:
  pull_request:
  push:

jobs:
  verify-real-case-authority-object-admission-decision-ledger:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Verify real case authority object admission decision ledger
        run: |
          bash scripts/verify-real-case-authority-object-admission-decision-ledger.sh
          python3 -m unittest tests/test_real_case_authority_object_admission_decision_ledger.py
          bash scripts/verify-object-registry-fresh.sh
