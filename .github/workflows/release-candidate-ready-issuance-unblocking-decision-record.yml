name: release-candidate-ready-issuance-unblocking-decision-record

on:
  push:
  pull_request:

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: python3 -m unittest tests/test_release_candidate_ready_issuance_unblocking_decision_record.py
      - run: bash scripts/verify-release-candidate-ready-issuance-unblocking-decision-record.sh
