name: second future editorial timeline authority object instantiation record

on:
  push:
  pull_request:

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Verify second future editorial timeline authority object instantiation record
        run: bash scripts/verify-second-future-editorial-timeline-authority-object-instantiation-record.sh
      - name: Unit test
        run: python3 -m unittest tests/test_second_future_editorial_timeline_authority_object_instantiation_record.py
      - name: Verify all
        run: bash scripts/verify-all.sh
