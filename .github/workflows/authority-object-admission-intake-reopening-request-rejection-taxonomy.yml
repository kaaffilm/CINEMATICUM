name: authority-object-admission-intake-reopening-request-rejection-taxonomy

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  authority-object-admission-intake-reopening-request-rejection-taxonomy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Verify authority object admission intake reopening request rejection taxonomy
        run: |
          bash scripts/verify-authority-object-admission-intake-reopening-request-rejection-taxonomy.sh
          python3 -m unittest tests/test_authority_object_admission_intake_reopening_request_rejection_taxonomy.py
