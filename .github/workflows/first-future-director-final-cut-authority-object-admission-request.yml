name: first-future-director-final-cut-authority-object-admission-request

on:
pull_request:
push:
branches:
- main

jobs:
verify:
runs-on: ubuntu-latest
steps:
- uses: actions/checkout@v4
- name: Verify first future director final cut authority object admission request
run: |
bash scripts/verify-first-future-director-final-cut-authority-object-admission-request.sh
python3 -m unittest tests/test_first_future_director_final_cut_authority_object_admission_request.py
