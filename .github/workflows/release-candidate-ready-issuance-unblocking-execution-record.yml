name: release-candidate-ready-issuance-unblocking-execution-record

on:
  push:
  pull_request:

jobs:
  verify-release-candidate-ready-issuance-unblocking-execution-record:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.x"
      - run: bash scripts/verify-release-candidate-ready-issuance-unblocking-execution-record.sh
