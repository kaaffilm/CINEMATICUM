name: authority-object-admission-request-rejection-corpus

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  authority-object-admission-request-rejection-corpus:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Verify authority object admission request rejection corpus
        run: bash scripts/verify-authority-object-admission-request-rejection-corpus.sh
      - name: Run authority object admission request rejection corpus tests
        run: python3 -m unittest tests/test_authority_object_admission_request_rejection_corpus.py
