name: real-case-authority-object-admission-request-validator

on:
pull_request:
push:

jobs:
verify:
runs-on: ubuntu-latest
steps:
- uses: actions/checkout@v4
- name: Verify real case authority object admission request validator
run: bash scripts/verify-real-case-authority-object-admission-request-validator.sh
- name: Unit test real case authority object admission request validator
run: python3 -m unittest tests/test_real_case_authority_object_admission_request_validator.py
