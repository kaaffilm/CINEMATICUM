name: release-candidate-admissibility-verdict-record

on:
  pull_request:
  push:
    branches: [ main ]

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Verify release-candidate-admissibility-verdict-record
        run: bash scripts/verify-release-candidate-admissibility-verdict-record.sh
