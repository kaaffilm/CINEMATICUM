name: release-candidate-outsider-replay-passage-record

on:
  pull_request:
  push:
    branches: [ main ]

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Verify release-candidate-outsider-replay-passage-record
        run: bash scripts/verify-release-candidate-outsider-replay-passage-record.sh
