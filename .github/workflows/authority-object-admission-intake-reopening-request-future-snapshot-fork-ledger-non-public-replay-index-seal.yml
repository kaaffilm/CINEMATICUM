name: authority-object-admission-intake-reopening-request-future-snapshot-fork-ledger-non-public-replay-index-seal

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  verify-non-public-replay-index-seal:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Verify non-public-replay-index seal
        run: bash scripts/verify-authority-object-admission-intake-reopening-request-future-snapshot-fork-ledger-non-public-replay-index-seal.sh
      - name: Unit test
        run: python -m unittest tests.test_authority_object_admission_intake_reopening_request_future_snapshot_fork_ledger_non_public_replay_index_seal
