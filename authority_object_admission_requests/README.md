# CINEMATICUM — Authority Object Admission Requests

This directory is reserved for future public admission requests.

At this stage it intentionally contains no request JSON.

No authority object may be instantiated silently.

A future request must be public, typed, case-bound, template-bound, actor-bound, timestamped, and rejected or accepted by verification before any authority object can be placed under `authority_objects/`.
