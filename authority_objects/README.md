# CINEMATICUM — Authority Objects Directory

This directory is reserved for future instantiated authority objects.

At this stage it intentionally contains no instantiated authority object JSON.

Templates live under:

    templates/authority_objects/

Templates are not authority objects.

An authority object may only appear here after the instantiation gate is satisfied by explicit accountable act, required fields, current-state compatibility, and verification.
