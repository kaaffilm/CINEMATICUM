#!/usr/bin/env bash
set -euo pipefail
python3 - <<'PY2'
import json
from pathlib import Path

root = Path(".")
obj = "RELEASE_CANDIDATE_READY_ISSUANCE_UNBLOCKING_REQUEST"
r = json.loads((root / f"{obj}.json").read_text())
c = json.loads((root / "CASES/CASE_001_THE_LAST_RENDER" / f"{obj}_STATUS.json").read_text())

assert r["CURRENT_STATE"] == "RELEASE_CANDIDATE_READY"
assert r["REQUEST_OBJECT"] == obj
assert r["REQUEST_ID"] == "REQ_001_RELEASE_CANDIDATE_READY_ISSUANCE_UNBLOCKING_REQUEST"
assert r["REQUIRED_PRIOR_OBJECT"] == "RELEASE_CANDIDATE_READY_ISSUANCE_BLOCKADE_SEAL"
assert r["NEXT_REQUIRED_OBJECT"] == "RELEASE_CANDIDATE_READY_ISSUANCE_UNBLOCKING_DECISION_RECORD"

for k in [
    "RELEASE_CANDIDATE_READY",
    "BLOCKADE_SEAL_PRESENT",
    "ISSUANCE_UNBLOCKING_REQUEST_PRESENT",
    "REQUEST_IS_NOT_DECISION",
    "REQUEST_DOES_NOT_UNBLOCK_ISSUANCE",
    "REQUEST_DOES_NOT_ISSUE_MOTION_PICTURE",
    "REQUEST_DOES_NOT_ADMIT_MEDIA",
    "DECISION_RECORD_REQUIRED_BEFORE_ISSUANCE_UNBLOCKING",
    "CURRENT_STATE_UNCHANGED",
]:
    assert r[k] is True, k

for k in ["MAY_ADVANCE_NOW", "ISSUANCE_UNBLOCKED", "ISSUED", "MEDIA_PRESENT"]:
    assert r[k] is False, k
    assert c[k] is False, "case:" + k

print("CINEMATICUM RELEASE CANDIDATE READY ISSUANCE UNBLOCKING REQUEST: PASS")
for k in [
    "CURRENT_STATE", "REQUEST_SCOPE", "REQUEST_OBJECT", "REQUEST_ID",
    "REQUESTED_NEXT_STATE", "REQUIRED_PRIOR_OBJECT", "RELEASE_CANDIDATE_READY",
    "BLOCKADE_SEAL_PRESENT", "ISSUANCE_UNBLOCKING_REQUEST_PRESENT",
    "REQUEST_IS_NOT_DECISION", "REQUEST_DOES_NOT_UNBLOCK_ISSUANCE",
    "DECISION_RECORD_REQUIRED_BEFORE_ISSUANCE_UNBLOCKING",
    "MAY_ADVANCE_NOW", "ISSUANCE_UNBLOCKED", "ISSUED", "MEDIA_PRESENT",
    "CURRENT_STATE_UNCHANGED", "NEXT_REQUIRED_OBJECT"
]:
    v = r[k]
    print(f"{k}={str(v).lower() if isinstance(v, bool) else v}")
PY2
